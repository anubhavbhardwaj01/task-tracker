# railway.toml for BACKEND service
[build]
builder = "NIXPACKS"
buildCommand = "NPM_CONFIG_CACHE=/tmp/.npm npm ci"

[deploy]
startCommand = "node server.js"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
